apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ingress-cert
spec:
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
  secretName: ingress-cert
  commonName: "*.apocrypha.in"
  dnsNames:
    - "*.apocrypha.in"
