apiVersion: apps/v1
kind: Deployment
metadata:
  name: vitess-operator
spec:
  template:
    spec:
      containers:
        - name: vitess-operator
          env:
            - name: WATCH_NAMESPACE
              value: "vitess-dev"
