apiVersion: v1
kind: Service
metadata:
  name: api-server
  labels:
    app: api-server
    service: api-server
spec:
  ports:
    - name: http
      port: 80
      targetPort: 3000
  selector:
    app: api-server
